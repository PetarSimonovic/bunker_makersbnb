CREATE TABLE users(id SERIAL PRIMARY KEY, username VARCHAR(60), password VARCHAR(20), email VARCHAR(50));

"Ensure "users" table is created then:"

CREATE TABLE users(user_id SERIAL PRIMARY KEY, username VARCHAR(60), password VARCHAR(20), email VARCHAR(50));